export const mergeArrays = function(origin, versions) {
    var result = [];
    var lens = versions.map(o => o == null ? 0 : o.length);
    lens.unshift(origin == null ? 0 : origin.length);
    var i = 0;
    //while(true) {

    //}
    return mergeArrays.toString();
};

var f = function () {
    throw "";
};

f();

export default {
     mergeArrays
};
